interface:
  display_name: "Astra-Ultra"
  short_description: "Universal Token Economizer & Reasoning Governor for Codex (Any Model, Any Effort)"
  default_prompt: "Use $astra-ultra to solve this task with maximum token efficiency, bounded inspection, and optimized test-time compute."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: mcp
      server_name: "astra-symbol-server"
      description: "Surgical AST skeletons, RepoMaps, and bounded window slices"
